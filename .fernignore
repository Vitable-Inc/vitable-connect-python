# Files Fern must not overwrite or delete when regenerating this repo.
# ci.yml and publish-pypi.yml are intentionally NOT listed: publishing moved
# into Fern's generated ci.yml (token: OIDC), so the next regeneration
# replaces our patched ci.yml and deletes publish-pypi.yml.
LICENSE
SECURITY.md
changelog.md

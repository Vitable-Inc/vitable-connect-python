# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.access_token_response import AccessTokenResponse
from ..types.bound_entity import BoundEntity
from ..types.grant_type import GrantType
from .raw_client import AsyncRawAuthClient, RawAuthClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class AuthClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAuthClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAuthClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAuthClient
        """
        return self._raw_client

    def issue_access_token(
        self,
        *,
        grant_type: GrantType,
        bound_entity: typing.Optional[BoundEntity] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccessTokenResponse:
        """
        Issues a short-lived access token from the authenticated API key. Access tokens can optionally be bound to a specific employer or employee for scoped access. Tokens expire after 15 minutes.

        Parameters
        ----------
        grant_type : GrantType
            Token issuance flow. Currently only 'client_credentials' supported.

            * `client_credentials` - client_credentials

        bound_entity : typing.Optional[BoundEntity]
            Optional entity to bind the token to for scoped access

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccessTokenResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.auth.issue_access_token(
            grant_type="client_credentials",
        )
        """
        _response = self._raw_client.issue_access_token(
            grant_type=grant_type, bound_entity=bound_entity, request_options=request_options
        )
        return _response.data


class AsyncAuthClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAuthClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAuthClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAuthClient
        """
        return self._raw_client

    async def issue_access_token(
        self,
        *,
        grant_type: GrantType,
        bound_entity: typing.Optional[BoundEntity] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AccessTokenResponse:
        """
        Issues a short-lived access token from the authenticated API key. Access tokens can optionally be bound to a specific employer or employee for scoped access. Tokens expire after 15 minutes.

        Parameters
        ----------
        grant_type : GrantType
            Token issuance flow. Currently only 'client_credentials' supported.

            * `client_credentials` - client_credentials

        bound_entity : typing.Optional[BoundEntity]
            Optional entity to bind the token to for scoped access

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AccessTokenResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.auth.issue_access_token(
                grant_type="client_credentials",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.issue_access_token(
            grant_type=grant_type, bound_entity=bound_entity, request_options=request_options
        )
        return _response.data

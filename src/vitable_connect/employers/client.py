# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.access_method import AccessMethod
from ..types.additional_access_method import AdditionalAccessMethod
from ..types.benefit_family_param_item import BenefitFamilyParamItem
from ..types.benefit_lifecycle_stage_item import BenefitLifecycleStageItem
from ..types.benefit_plan_year_id import BenefitPlanYearId
from ..types.census_sync_detail_response import CensusSyncDetailResponse
from ..types.census_sync_employee_request import CensusSyncEmployeeRequest
from ..types.classification_correction_source import ClassificationCorrectionSource
from ..types.deduction_frequency import DeductionFrequency
from ..types.election_status_item import ElectionStatusItem
from ..types.employee import Employee
from ..types.employee_list_response import EmployeeListResponse
from ..types.employee_search import EmployeeSearch
from ..types.employee_status import EmployeeStatus
from ..types.employer_address_input import EmployerAddressInput
from ..types.employer_benefit_plan_year_response import EmployerBenefitPlanYearResponse
from ..types.employer_benefit_plan_years_list_response import EmployerBenefitPlanYearsListResponse
from ..types.employer_hris_response import EmployerHrisResponse
from ..types.employer_id import EmployerId
from ..types.employer_invoice_pdf_response import EmployerInvoicePdfResponse
from ..types.employer_invoices_list_response import EmployerInvoicesListResponse
from ..types.employer_payroll_deduction_statement_list_response import EmployerPayrollDeductionStatementListResponse
from ..types.employer_response import EmployerResponse
from ..types.employer_search import EmployerSearch
from ..types.employer_settings_response import EmployerSettingsResponse
from ..types.hris_status_item import HrisStatusItem
from ..types.include_cancelled import IncludeCancelled
from ..types.invoice_id import InvoiceId
from ..types.limit import Limit
from ..types.missing_employee_resolution import MissingEmployeeResolution
from ..types.offset import Offset
from ..types.organization_employer import OrganizationEmployer
from ..types.organization_employer_list_response import OrganizationEmployerListResponse
from ..types.organization_hris_providers_response import OrganizationHrisProvidersResponse
from ..types.page import Page
from ..types.payroll_access_setup_status_response import PayrollAccessSetupStatusResponse
from ..types.payroll_deduction_statement import PayrollDeductionStatement
from ..types.payroll_integration_email_response import PayrollIntegrationEmailResponse
from ..types.plan_year_enrollment import PlanYearEnrollment
from ..types.plan_year_enrollment_list_response import PlanYearEnrollmentListResponse
from ..types.remaining_employee_action import RemainingEmployeeAction
from ..types.search import Search
from ..types.update_employer_address_input import UpdateEmployerAddressInput
from .raw_client import AsyncRawEmployersClient, RawEmployersClient

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class EmployersClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawEmployersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawEmployersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawEmployersClient
        """
        return self._raw_client

    def list(
        self,
        *,
        benefit_family: typing.Optional[
            typing.Union[BenefitFamilyParamItem, typing.Sequence[BenefitFamilyParamItem]]
        ] = None,
        benefit_lifecycle_stage: typing.Optional[
            typing.Union[BenefitLifecycleStageItem, typing.Sequence[BenefitLifecycleStageItem]]
        ] = None,
        hris_provider: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        hris_status: typing.Optional[typing.Union[HrisStatusItem, typing.Sequence[HrisStatusItem]]] = None,
        include_cancelled: typing.Optional[IncludeCancelled] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        search: typing.Optional[Search] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[OrganizationEmployer, OrganizationEmployerListResponse]:
        """
        Returns the caller's employer book — every employer with its computed columns (enrollment-rate summary, benefit-family tags, HRIS connection, benefit-lifecycle stage) merged with the employer's flat CRM fields (legal name, EIN, contact, address, timestamps). The book is derived from the authenticated principal: one organization's employers, or every organization's for a caller whose reach is not a single organization. Supports search by display name, legal name, or exact EIN, employer id or contact email, benefit-family/lifecycle/HRIS filters, and page/limit pagination.

        Parameters
        ----------
        benefit_family : typing.Optional[typing.Union[BenefitFamilyParamItem, typing.Sequence[BenefitFamilyParamItem]]]
            Filter to employers with at least one active benefit in these families.

        benefit_lifecycle_stage : typing.Optional[typing.Union[BenefitLifecycleStageItem, typing.Sequence[BenefitLifecycleStageItem]]]
            Filter to employers in one of these computed benefit-lifecycle stages.

        hris_provider : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to employers whose HRIS connection is with one of these payroll providers (e.g. `ADP RUN`). Matched case-insensitively; free text, so read the available values from the HRIS-providers endpoint rather than assuming a fixed set.

        hris_status : typing.Optional[typing.Union[HrisStatusItem, typing.Sequence[HrisStatusItem]]]
            Filter to employers whose HRIS connection is in one of these statuses.

        include_cancelled : typing.Optional[IncludeCancelled]
            Include cancelled employers (hidden by default unless their stage is explicitly requested).

        limit : typing.Optional[Limit]
            Items per page.

        page : typing.Optional[Page]
            Page number.

        search : typing.Optional[Search]
            Employer filter. Matches the display name or the legal name case-insensitively as a substring, or one of these exactly: the EIN (with or without its dash), the employer id, or the contact email of one of the employer's non-disabled admins.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[OrganizationEmployer, OrganizationEmployerListResponse]


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        response = client.employers.list(
            limit=20,
            page=1,
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            benefit_family=benefit_family,
            benefit_lifecycle_stage=benefit_lifecycle_stage,
            hris_provider=hris_provider,
            hris_status=hris_status,
            include_cancelled=include_cancelled,
            limit=limit,
            page=page,
            search=search,
            request_options=request_options,
        )

    def create(
        self,
        *,
        name: str,
        legal_name: str,
        ein: str,
        email: str,
        address: EmployerAddressInput,
        phone_number: typing.Optional[str] = OMIT,
        reference_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerResponse:
        """
        Creates a new employer for the authenticated organization. Requires employer name, legal name, EIN, email, and address information. Returns the created employer with its assigned ID.

        Parameters
        ----------
        name : str
            Employer display name

        legal_name : str
            Legal business name

        ein : str
            Employer Identification Number (format: XX-XXXXXXX)

        email : str
            Email address for billing and communications

        address : EmployerAddressInput
            Employer address

        phone_number : typing.Optional[str]
            Employer phone number (10-digit US format, e.g. 5551234567)

        reference_id : typing.Optional[str]
            External reference ID for this employer

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerResponse


        Examples
        --------
        from vitable_connect import EmployerAddressInput, VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.create(
            name="NewCo Industries",
            legal_name="NewCo Industries LLC",
            ein="12-3456789",
            email="hr@newco.com",
            address=EmployerAddressInput(
                address_line1="789 Business Blvd",
                address_line2="Floor 5",
                city="Seattle",
                state="WA",
                zipcode="98101",
            ),
            phone_number="2065550100",
            reference_id="partner-emp-001",
        )
        """
        _response = self._raw_client.create(
            name=name,
            legal_name=legal_name,
            ein=ein,
            email=email,
            address=address,
            phone_number=phone_number,
            reference_id=reference_id,
            request_options=request_options,
        )
        return _response.data

    def get(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerResponse:
        """
        Retrieves detailed information for a specific employer by ID. The employer must belong to the authenticated organization.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.get(
            employer_id="empr_abc123def456",
        )
        """
        _response = self._raw_client.get(employer_id, request_options=request_options)
        return _response.data

    def update(
        self,
        employer_id: EmployerId,
        *,
        name: typing.Optional[str] = OMIT,
        legal_name: typing.Optional[str] = OMIT,
        address: typing.Optional[UpdateEmployerAddressInput] = OMIT,
        active: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerResponse:
        """
        Updates an existing employer. All fields are optional — only provided fields are updated. PO Box addresses are rejected.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        name : typing.Optional[str]
            Employer display name

        legal_name : typing.Optional[str]
            Legal business name

        address : typing.Optional[UpdateEmployerAddressInput]
            Employer address

        active : typing.Optional[bool]
            Whether the employer is active

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.update(
            employer_id="empr_abc123def456",
        )
        """
        _response = self._raw_client.update(
            employer_id,
            name=name,
            legal_name=legal_name,
            address=address,
            active=active,
            request_options=request_options,
        )
        return _response.data

    def list_benefit_plan_years(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerBenefitPlanYearsListResponse:
        """
        Returns the employer's benefit plan years (all years, or one when `year` is given), each with its benefits, offered states, benefit families, and the year-level enrollment roll-up. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerBenefitPlanYearsListResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.list_benefit_plan_years(
            employer_id="empr_abc123def456",
        )
        """
        _response = self._raw_client.list_benefit_plan_years(employer_id, request_options=request_options)
        return _response.data

    def get_benefit_plan_year(
        self,
        employer_id: EmployerId,
        benefit_plan_year_id: BenefitPlanYearId,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerBenefitPlanYearResponse:
        """
        Returns one benefit plan year in full — its benefit details plus the per-benefit enrollment rate and SPD link — addressed by its `benefit_plan_year_id`. The caller must be authorized for the employer; an unknown or unauthorized plan year returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        benefit_plan_year_id : BenefitPlanYearId
            Unique benefit-plan-year identifier (plyr_*).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerBenefitPlanYearResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.get_benefit_plan_year(
            employer_id="empr_abc123def456",
            benefit_plan_year_id="plyr_abc123def456",
        )
        """
        _response = self._raw_client.get_benefit_plan_year(
            employer_id, benefit_plan_year_id, request_options=request_options
        )
        return _response.data

    def list_benefit_plan_year_enrollments(
        self,
        employer_id: EmployerId,
        benefit_plan_year_id: BenefitPlanYearId,
        *,
        election_status: typing.Optional[typing.Union[ElectionStatusItem, typing.Sequence[ElectionStatusItem]]] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        search: typing.Optional[EmployerSearch] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PlanYearEnrollment, PlanYearEnrollmentListResponse]:
        """
        Returns a paginated list of every member with an enrollment in one of an employer's plan years, any election status: what they elected, where their coverage stands, dependent count, carrier, plan, tier, and the plan's total monthly cost. The caller must be authorized for the employer `empr_<...>`; an unknown or unauthorized employer, or an unknown plan year `plyr_<...>`, returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*).

        benefit_plan_year_id : BenefitPlanYearId
            Unique benefit-plan-year identifier (plyr_*).

        election_status : typing.Optional[typing.Union[ElectionStatusItem, typing.Sequence[ElectionStatusItem]]]
            Filter by election status. Repeat the parameter to match several.

        limit : typing.Optional[Limit]
            Items per page (default: 20, max: 100)

        page : typing.Optional[Page]
            Page number (default: 1)

        search : typing.Optional[EmployerSearch]
            Case-insensitive search. Matches member name partially, and the `member_id` exactly — either your own reference id or the prefixed `grpmbr_<...>` id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PlanYearEnrollment, PlanYearEnrollmentListResponse]


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        response = client.employers.list_benefit_plan_year_enrollments(
            employer_id="empr_abc123def456",
            benefit_plan_year_id="plyr_abc123def456",
            limit=20,
            page=1,
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_benefit_plan_year_enrollments(
            employer_id,
            benefit_plan_year_id,
            election_status=election_status,
            limit=limit,
            page=page,
            search=search,
            request_options=request_options,
        )

    def submit_census_sync(
        self,
        employer_id: EmployerId,
        *,
        employees: typing.Sequence[CensusSyncEmployeeRequest],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CensusSyncDetailResponse:
        """
        Submits a census sync payload for the specified employer. The employees in the payload will be queued for processing. Returns an accepted response with the timestamp of acceptance.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        employees : typing.Sequence[CensusSyncEmployeeRequest]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CensusSyncDetailResponse


        Examples
        --------
        import datetime

        from vitable_connect import (
            CensusSyncEmployeeAddressRequest,
            CensusSyncEmployeeRequest,
            VitableConnect,
        )

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.submit_census_sync(
            employer_id="empr_abc123def456",
            employees=[
                CensusSyncEmployeeRequest(
                    reference_id="EMP-001",
                    first_name="Jane",
                    last_name="Doe",
                    date_of_birth=datetime.date.fromisoformat(
                        "1990-05-15",
                    ),
                    email="jane.doe@acme.com",
                    phone="4155550100",
                    address=CensusSyncEmployeeAddressRequest(
                        address_line1="123 Main Street",
                        address_line2="Apt 4B",
                        city="San Francisco",
                        state="CA",
                        zipcode="94102",
                    ),
                    start_date=datetime.date.fromisoformat(
                        "2024-01-15",
                    ),
                    employee_class="Full Time",
                    compensation_type="Salary",
                ),
                CensusSyncEmployeeRequest(
                    first_name="John",
                    last_name="Smith",
                    date_of_birth=datetime.date.fromisoformat(
                        "1985-11-20",
                    ),
                    email="john.smith@acme.com",
                    phone="4155550101",
                    start_date=datetime.date.fromisoformat(
                        "2024-03-01",
                    ),
                    employee_class="Part Time",
                    compensation_type="Hourly",
                ),
            ],
        )
        """
        _response = self._raw_client.submit_census_sync(
            employer_id, employees=employees, request_options=request_options
        )
        return _response.data

    def list_employees(
        self,
        employer_id: EmployerId,
        *,
        employment_status: typing.Optional[EmployeeStatus] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        search: typing.Optional[EmployeeSearch] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[Employee, EmployeeListResponse]:
        """
        Retrieves a paginated list of employees for a specific employer. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404. Results are paginated using page and limit parameters and can be narrowed with a case-insensitive `search` (first name, last name, or email) and an `employment_status` filter (active or terminated). Each employee includes payroll deductions from the most recent statement period. When a new deduction statement is generated, previous period deductions are replaced.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        employment_status : typing.Optional[EmployeeStatus]
            Filter by employment status (active or terminated)

        limit : typing.Optional[Limit]
            Items per page (default: 20, max: 100)

        page : typing.Optional[Page]
            Page number (default: 1)

        search : typing.Optional[EmployeeSearch]
            Case-insensitive search across employee first name, last name, and email

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[Employee, EmployeeListResponse]


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        response = client.employers.list_employees(
            employer_id="empr_abc123def456",
            limit=20,
            page=1,
            search="jane",
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_employees(
            employer_id,
            employment_status=employment_status,
            limit=limit,
            page=page,
            search=search,
            request_options=request_options,
        )

    def get_hris(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerHrisResponse:
        """
        Returns the employer's HRIS connection — provider, status, last sync, and synced row count — or null when the employer has no integration. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerHrisResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.get_hris(
            employer_id="empr_abc123def456",
        )
        """
        _response = self._raw_client.get_hris(employer_id, request_options=request_options)
        return _response.data

    def list_invoices(
        self,
        employer_id: EmployerId,
        *,
        limit: typing.Optional[Limit] = None,
        offset: typing.Optional[Offset] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerInvoicesListResponse:
        """
        Returns a cursor-paginated page of the employer's billing invoices, newest first. Pass the `next_offset` from a previous page as `offset` to fetch the next page. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        limit : typing.Optional[Limit]
            Maximum number of invoices per page

        offset : typing.Optional[Offset]
            Opaque cursor from a previous page's next_offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerInvoicesListResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.list_invoices(
            employer_id="empr_abc123def456",
            limit=20,
        )
        """
        _response = self._raw_client.list_invoices(
            employer_id, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    def get_invoice_pdf(
        self, employer_id: EmployerId, invoice_id: InvoiceId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerInvoicePdfResponse:
        """
        Returns the time-limited PDF download link for a single invoice belonging to the employer's billing customer. `invoice_id` is the external Chargebee id (not a prefixed UUID). The caller must be authorized for the employer; an unknown or unauthorized employer or invoice returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        invoice_id : InvoiceId
            External Chargebee invoice id (not a prefixed UUID).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerInvoicePdfResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.get_invoice_pdf(
            employer_id="empr_abc123def456",
            invoice_id="INV-00042",
        )
        """
        _response = self._raw_client.get_invoice_pdf(employer_id, invoice_id, request_options=request_options)
        return _response.data

    def get_payroll_access_setup(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PayrollAccessSetupStatusResponse:
        """
        Return whether the employer has submitted payroll access setup.

        Parameters
        ----------
        employer_id : EmployerId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayrollAccessSetupStatusResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.get_payroll_access_setup(
            employer_id="empr_abc123def456",
        )
        """
        _response = self._raw_client.get_payroll_access_setup(employer_id, request_options=request_options)
        return _response.data

    def submit_payroll_access_setup(
        self,
        employer_id: EmployerId,
        *,
        employees_in_payroll_acknowledged: bool,
        payroll_data_impacts_eligibility_acknowledged: bool,
        classifications_accurate: bool,
        all_benefit_eligible_employees_present: bool,
        is_controlled_group: bool,
        access_method: AccessMethod,
        has_additional_payroll_system: bool,
        classification_correction_source: typing.Optional[ClassificationCorrectionSource] = OMIT,
        misclassified_employee_names: typing.Optional[typing.Sequence[str]] = OMIT,
        remaining_employee_action: typing.Optional[RemainingEmployeeAction] = OMIT,
        missing_employee_resolution: typing.Optional[MissingEmployeeResolution] = OMIT,
        same_payroll_covers_other_eins: typing.Optional[bool] = OMIT,
        login_url: typing.Optional[str] = OMIT,
        username: typing.Optional[str] = OMIT,
        phone: typing.Optional[str] = OMIT,
        password: typing.Optional[str] = OMIT,
        integration_confirmed: typing.Optional[bool] = OMIT,
        additional_access_method: typing.Optional[AdditionalAccessMethod] = OMIT,
        additional_login_url: typing.Optional[str] = OMIT,
        additional_username: typing.Optional[str] = OMIT,
        additional_phone: typing.Optional[str] = OMIT,
        additional_password: typing.Optional[str] = OMIT,
        additional_integration_confirmed: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PayrollAccessSetupStatusResponse:
        """
        Submit the employer's payroll access setup answers.

        Parameters
        ----------
        employer_id : EmployerId

        employees_in_payroll_acknowledged : bool
            Attestation that all benefit-eligible employees appear in the payroll system.

        payroll_data_impacts_eligibility_acknowledged : bool
            Attestation that changes to payroll data affect benefit eligibility.

        classifications_accurate : bool
            Attestation that employee classifications in payroll are accurate. Set `false` to report corrections in the fields below.

        all_benefit_eligible_employees_present : bool
            Attestation that every benefit-eligible employee is present in payroll.

        is_controlled_group : bool
            Whether this employer belongs to a controlled group of related entities.

        access_method : AccessMethod

        has_additional_payroll_system : bool
            Whether a second payroll system is in use. When `true`, supply the `additional_*` fields below.

        classification_correction_source : typing.Optional[ClassificationCorrectionSource]
            Where corrected classifications come from, when `classifications_accurate` is `false`.

        misclassified_employee_names : typing.Optional[typing.Sequence[str]]
            Names of employees whose payroll classification needs correcting.

        remaining_employee_action : typing.Optional[RemainingEmployeeAction]
            How to handle employees still missing from the payroll system.

        missing_employee_resolution : typing.Optional[MissingEmployeeResolution]
            How any missing employees will be added, when some are absent.

        same_payroll_covers_other_eins : typing.Optional[bool]
            Whether this payroll system also covers other EINs in the controlled group.

        login_url : typing.Optional[str]
            Sign-in URL for the payroll system.

        username : typing.Optional[str]
            Username Vitable should use to access the payroll system.

        phone : typing.Optional[str]
            Phone number used for payroll-system verification codes.

        password : typing.Optional[str]
            Password Vitable should use to access the payroll system.

        integration_confirmed : typing.Optional[bool]
            Whether the payroll integration has been confirmed as working.

        additional_access_method : typing.Optional[AdditionalAccessMethod]
            How Vitable will access the second payroll system.

        additional_login_url : typing.Optional[str]
            Sign-in URL for the second payroll system.

        additional_username : typing.Optional[str]
            Username Vitable should use for the second payroll system.

        additional_phone : typing.Optional[str]
            Phone number used for second payroll-system verification codes.

        additional_password : typing.Optional[str]
            Password Vitable should use for the second payroll system.

        additional_integration_confirmed : typing.Optional[bool]
            Whether the second payroll integration has been confirmed as working.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayrollAccessSetupStatusResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.submit_payroll_access_setup(
            employer_id="empr_abc123def456",
            employees_in_payroll_acknowledged=True,
            payroll_data_impacts_eligibility_acknowledged=True,
            classifications_accurate=True,
            all_benefit_eligible_employees_present=True,
            is_controlled_group=True,
            access_method="SELF_SETUP",
            has_additional_payroll_system=True,
        )
        """
        _response = self._raw_client.submit_payroll_access_setup(
            employer_id,
            employees_in_payroll_acknowledged=employees_in_payroll_acknowledged,
            payroll_data_impacts_eligibility_acknowledged=payroll_data_impacts_eligibility_acknowledged,
            classifications_accurate=classifications_accurate,
            all_benefit_eligible_employees_present=all_benefit_eligible_employees_present,
            is_controlled_group=is_controlled_group,
            access_method=access_method,
            has_additional_payroll_system=has_additional_payroll_system,
            classification_correction_source=classification_correction_source,
            misclassified_employee_names=misclassified_employee_names,
            remaining_employee_action=remaining_employee_action,
            missing_employee_resolution=missing_employee_resolution,
            same_payroll_covers_other_eins=same_payroll_covers_other_eins,
            login_url=login_url,
            username=username,
            phone=phone,
            password=password,
            integration_confirmed=integration_confirmed,
            additional_access_method=additional_access_method,
            additional_login_url=additional_login_url,
            additional_username=additional_username,
            additional_phone=additional_phone,
            additional_password=additional_password,
            additional_integration_confirmed=additional_integration_confirmed,
            request_options=request_options,
        )
        return _response.data

    def list_payroll_deduction_statements(
        self,
        employer_id: EmployerId,
        *,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[PayrollDeductionStatement, EmployerPayrollDeductionStatementListResponse]:
        """
        Returns a paginated list of the employer's payroll-deduction statements, newest period first, each with its period, generation date, distinct employee count, total deduction, change-file link, and deduction frequency. Statements superseded by a later correction are excluded. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        limit : typing.Optional[Limit]
            Maximum number of statements per page

        page : typing.Optional[Page]
            Page number to retrieve (starts at 1)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[PayrollDeductionStatement, EmployerPayrollDeductionStatementListResponse]


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        response = client.employers.list_payroll_deduction_statements(
            employer_id="empr_abc123def456",
            limit=20,
            page=1,
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list_payroll_deduction_statements(
            employer_id, limit=limit, page=page, request_options=request_options
        )

    def ensure_payroll_integration_email(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PayrollIntegrationEmailResponse:
        """
        Provision and return the employer's payroll integration email.

        Parameters
        ----------
        employer_id : EmployerId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayrollIntegrationEmailResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.ensure_payroll_integration_email(
            employer_id="empr_abc123def456",
        )
        """
        _response = self._raw_client.ensure_payroll_integration_email(employer_id, request_options=request_options)
        return _response.data

    def update_settings(
        self,
        employer_id: EmployerId,
        *,
        pay_frequency: DeductionFrequency,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerSettingsResponse:
        """
        Updates configuration settings for a specific employer. The employer must belong to the authenticated organization.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        pay_frequency : DeductionFrequency

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerSettingsResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.update_settings(
            employer_id="empr_abc123def456",
            pay_frequency="bi_weekly",
        )
        """
        _response = self._raw_client.update_settings(
            employer_id, pay_frequency=pay_frequency, request_options=request_options
        )
        return _response.data

    def list_hris_providers(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> OrganizationHrisProvidersResponse:
        """
        Returns the distinct HRIS/payroll providers across the same book `GET /v1/employers` returns, sorted for display. Use these as the values for the employers list's `hris_provider` filter — filter on `provider`, show `provider_label`. The stored providers are free text, so they cannot be enumerated in advance.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        OrganizationHrisProvidersResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.employers.list_hris_providers()
        """
        _response = self._raw_client.list_hris_providers(request_options=request_options)
        return _response.data


class AsyncEmployersClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawEmployersClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawEmployersClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawEmployersClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        benefit_family: typing.Optional[
            typing.Union[BenefitFamilyParamItem, typing.Sequence[BenefitFamilyParamItem]]
        ] = None,
        benefit_lifecycle_stage: typing.Optional[
            typing.Union[BenefitLifecycleStageItem, typing.Sequence[BenefitLifecycleStageItem]]
        ] = None,
        hris_provider: typing.Optional[typing.Union[str, typing.Sequence[str]]] = None,
        hris_status: typing.Optional[typing.Union[HrisStatusItem, typing.Sequence[HrisStatusItem]]] = None,
        include_cancelled: typing.Optional[IncludeCancelled] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        search: typing.Optional[Search] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[OrganizationEmployer, OrganizationEmployerListResponse]:
        """
        Returns the caller's employer book — every employer with its computed columns (enrollment-rate summary, benefit-family tags, HRIS connection, benefit-lifecycle stage) merged with the employer's flat CRM fields (legal name, EIN, contact, address, timestamps). The book is derived from the authenticated principal: one organization's employers, or every organization's for a caller whose reach is not a single organization. Supports search by display name, legal name, or exact EIN, employer id or contact email, benefit-family/lifecycle/HRIS filters, and page/limit pagination.

        Parameters
        ----------
        benefit_family : typing.Optional[typing.Union[BenefitFamilyParamItem, typing.Sequence[BenefitFamilyParamItem]]]
            Filter to employers with at least one active benefit in these families.

        benefit_lifecycle_stage : typing.Optional[typing.Union[BenefitLifecycleStageItem, typing.Sequence[BenefitLifecycleStageItem]]]
            Filter to employers in one of these computed benefit-lifecycle stages.

        hris_provider : typing.Optional[typing.Union[str, typing.Sequence[str]]]
            Filter to employers whose HRIS connection is with one of these payroll providers (e.g. `ADP RUN`). Matched case-insensitively; free text, so read the available values from the HRIS-providers endpoint rather than assuming a fixed set.

        hris_status : typing.Optional[typing.Union[HrisStatusItem, typing.Sequence[HrisStatusItem]]]
            Filter to employers whose HRIS connection is in one of these statuses.

        include_cancelled : typing.Optional[IncludeCancelled]
            Include cancelled employers (hidden by default unless their stage is explicitly requested).

        limit : typing.Optional[Limit]
            Items per page.

        page : typing.Optional[Page]
            Page number.

        search : typing.Optional[Search]
            Employer filter. Matches the display name or the legal name case-insensitively as a substring, or one of these exactly: the EIN (with or without its dash), the employer id, or the contact email of one of the employer's non-disabled admins.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[OrganizationEmployer, OrganizationEmployerListResponse]


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.employers.list(
                limit=20,
                page=1,
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            benefit_family=benefit_family,
            benefit_lifecycle_stage=benefit_lifecycle_stage,
            hris_provider=hris_provider,
            hris_status=hris_status,
            include_cancelled=include_cancelled,
            limit=limit,
            page=page,
            search=search,
            request_options=request_options,
        )

    async def create(
        self,
        *,
        name: str,
        legal_name: str,
        ein: str,
        email: str,
        address: EmployerAddressInput,
        phone_number: typing.Optional[str] = OMIT,
        reference_id: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerResponse:
        """
        Creates a new employer for the authenticated organization. Requires employer name, legal name, EIN, email, and address information. Returns the created employer with its assigned ID.

        Parameters
        ----------
        name : str
            Employer display name

        legal_name : str
            Legal business name

        ein : str
            Employer Identification Number (format: XX-XXXXXXX)

        email : str
            Email address for billing and communications

        address : EmployerAddressInput
            Employer address

        phone_number : typing.Optional[str]
            Employer phone number (10-digit US format, e.g. 5551234567)

        reference_id : typing.Optional[str]
            External reference ID for this employer

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect, EmployerAddressInput

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.create(
                name="NewCo Industries",
                legal_name="NewCo Industries LLC",
                ein="12-3456789",
                email="hr@newco.com",
                address=EmployerAddressInput(
                    address_line1="789 Business Blvd",
                    address_line2="Floor 5",
                    city="Seattle",
                    state="WA",
                    zipcode="98101",
                ),
                phone_number="2065550100",
                reference_id="partner-emp-001",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            name=name,
            legal_name=legal_name,
            ein=ein,
            email=email,
            address=address,
            phone_number=phone_number,
            reference_id=reference_id,
            request_options=request_options,
        )
        return _response.data

    async def get(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerResponse:
        """
        Retrieves detailed information for a specific employer by ID. The employer must belong to the authenticated organization.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.get(
                employer_id="empr_abc123def456",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(employer_id, request_options=request_options)
        return _response.data

    async def update(
        self,
        employer_id: EmployerId,
        *,
        name: typing.Optional[str] = OMIT,
        legal_name: typing.Optional[str] = OMIT,
        address: typing.Optional[UpdateEmployerAddressInput] = OMIT,
        active: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerResponse:
        """
        Updates an existing employer. All fields are optional — only provided fields are updated. PO Box addresses are rejected.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        name : typing.Optional[str]
            Employer display name

        legal_name : typing.Optional[str]
            Legal business name

        address : typing.Optional[UpdateEmployerAddressInput]
            Employer address

        active : typing.Optional[bool]
            Whether the employer is active

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.update(
                employer_id="empr_abc123def456",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            employer_id,
            name=name,
            legal_name=legal_name,
            address=address,
            active=active,
            request_options=request_options,
        )
        return _response.data

    async def list_benefit_plan_years(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerBenefitPlanYearsListResponse:
        """
        Returns the employer's benefit plan years (all years, or one when `year` is given), each with its benefits, offered states, benefit families, and the year-level enrollment roll-up. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerBenefitPlanYearsListResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.list_benefit_plan_years(
                employer_id="empr_abc123def456",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_benefit_plan_years(employer_id, request_options=request_options)
        return _response.data

    async def get_benefit_plan_year(
        self,
        employer_id: EmployerId,
        benefit_plan_year_id: BenefitPlanYearId,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerBenefitPlanYearResponse:
        """
        Returns one benefit plan year in full — its benefit details plus the per-benefit enrollment rate and SPD link — addressed by its `benefit_plan_year_id`. The caller must be authorized for the employer; an unknown or unauthorized plan year returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        benefit_plan_year_id : BenefitPlanYearId
            Unique benefit-plan-year identifier (plyr_*).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerBenefitPlanYearResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.get_benefit_plan_year(
                employer_id="empr_abc123def456",
                benefit_plan_year_id="plyr_abc123def456",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_benefit_plan_year(
            employer_id, benefit_plan_year_id, request_options=request_options
        )
        return _response.data

    async def list_benefit_plan_year_enrollments(
        self,
        employer_id: EmployerId,
        benefit_plan_year_id: BenefitPlanYearId,
        *,
        election_status: typing.Optional[typing.Union[ElectionStatusItem, typing.Sequence[ElectionStatusItem]]] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        search: typing.Optional[EmployerSearch] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PlanYearEnrollment, PlanYearEnrollmentListResponse]:
        """
        Returns a paginated list of every member with an enrollment in one of an employer's plan years, any election status: what they elected, where their coverage stands, dependent count, carrier, plan, tier, and the plan's total monthly cost. The caller must be authorized for the employer `empr_<...>`; an unknown or unauthorized employer, or an unknown plan year `plyr_<...>`, returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*).

        benefit_plan_year_id : BenefitPlanYearId
            Unique benefit-plan-year identifier (plyr_*).

        election_status : typing.Optional[typing.Union[ElectionStatusItem, typing.Sequence[ElectionStatusItem]]]
            Filter by election status. Repeat the parameter to match several.

        limit : typing.Optional[Limit]
            Items per page (default: 20, max: 100)

        page : typing.Optional[Page]
            Page number (default: 1)

        search : typing.Optional[EmployerSearch]
            Case-insensitive search. Matches member name partially, and the `member_id` exactly — either your own reference id or the prefixed `grpmbr_<...>` id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PlanYearEnrollment, PlanYearEnrollmentListResponse]


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.employers.list_benefit_plan_year_enrollments(
                employer_id="empr_abc123def456",
                benefit_plan_year_id="plyr_abc123def456",
                limit=20,
                page=1,
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_benefit_plan_year_enrollments(
            employer_id,
            benefit_plan_year_id,
            election_status=election_status,
            limit=limit,
            page=page,
            search=search,
            request_options=request_options,
        )

    async def submit_census_sync(
        self,
        employer_id: EmployerId,
        *,
        employees: typing.Sequence[CensusSyncEmployeeRequest],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> CensusSyncDetailResponse:
        """
        Submits a census sync payload for the specified employer. The employees in the payload will be queued for processing. Returns an accepted response with the timestamp of acceptance.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        employees : typing.Sequence[CensusSyncEmployeeRequest]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CensusSyncDetailResponse


        Examples
        --------
        import asyncio
        import datetime

        from vitable_connect import (
            AsyncVitableConnect,
            CensusSyncEmployeeAddressRequest,
            CensusSyncEmployeeRequest,
        )

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.submit_census_sync(
                employer_id="empr_abc123def456",
                employees=[
                    CensusSyncEmployeeRequest(
                        reference_id="EMP-001",
                        first_name="Jane",
                        last_name="Doe",
                        date_of_birth=datetime.date.fromisoformat(
                            "1990-05-15",
                        ),
                        email="jane.doe@acme.com",
                        phone="4155550100",
                        address=CensusSyncEmployeeAddressRequest(
                            address_line1="123 Main Street",
                            address_line2="Apt 4B",
                            city="San Francisco",
                            state="CA",
                            zipcode="94102",
                        ),
                        start_date=datetime.date.fromisoformat(
                            "2024-01-15",
                        ),
                        employee_class="Full Time",
                        compensation_type="Salary",
                    ),
                    CensusSyncEmployeeRequest(
                        first_name="John",
                        last_name="Smith",
                        date_of_birth=datetime.date.fromisoformat(
                            "1985-11-20",
                        ),
                        email="john.smith@acme.com",
                        phone="4155550101",
                        start_date=datetime.date.fromisoformat(
                            "2024-03-01",
                        ),
                        employee_class="Part Time",
                        compensation_type="Hourly",
                    ),
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_census_sync(
            employer_id, employees=employees, request_options=request_options
        )
        return _response.data

    async def list_employees(
        self,
        employer_id: EmployerId,
        *,
        employment_status: typing.Optional[EmployeeStatus] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        search: typing.Optional[EmployeeSearch] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[Employee, EmployeeListResponse]:
        """
        Retrieves a paginated list of employees for a specific employer. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404. Results are paginated using page and limit parameters and can be narrowed with a case-insensitive `search` (first name, last name, or email) and an `employment_status` filter (active or terminated). Each employee includes payroll deductions from the most recent statement period. When a new deduction statement is generated, previous period deductions are replaced.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        employment_status : typing.Optional[EmployeeStatus]
            Filter by employment status (active or terminated)

        limit : typing.Optional[Limit]
            Items per page (default: 20, max: 100)

        page : typing.Optional[Page]
            Page number (default: 1)

        search : typing.Optional[EmployeeSearch]
            Case-insensitive search across employee first name, last name, and email

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[Employee, EmployeeListResponse]


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.employers.list_employees(
                employer_id="empr_abc123def456",
                limit=20,
                page=1,
                search="jane",
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_employees(
            employer_id,
            employment_status=employment_status,
            limit=limit,
            page=page,
            search=search,
            request_options=request_options,
        )

    async def get_hris(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerHrisResponse:
        """
        Returns the employer's HRIS connection — provider, status, last sync, and synced row count — or null when the employer has no integration. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerHrisResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.get_hris(
                employer_id="empr_abc123def456",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_hris(employer_id, request_options=request_options)
        return _response.data

    async def list_invoices(
        self,
        employer_id: EmployerId,
        *,
        limit: typing.Optional[Limit] = None,
        offset: typing.Optional[Offset] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerInvoicesListResponse:
        """
        Returns a cursor-paginated page of the employer's billing invoices, newest first. Pass the `next_offset` from a previous page as `offset` to fetch the next page. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        limit : typing.Optional[Limit]
            Maximum number of invoices per page

        offset : typing.Optional[Offset]
            Opaque cursor from a previous page's next_offset

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerInvoicesListResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.list_invoices(
                employer_id="empr_abc123def456",
                limit=20,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_invoices(
            employer_id, limit=limit, offset=offset, request_options=request_options
        )
        return _response.data

    async def get_invoice_pdf(
        self, employer_id: EmployerId, invoice_id: InvoiceId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> EmployerInvoicePdfResponse:
        """
        Returns the time-limited PDF download link for a single invoice belonging to the employer's billing customer. `invoice_id` is the external Chargebee id (not a prefixed UUID). The caller must be authorized for the employer; an unknown or unauthorized employer or invoice returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        invoice_id : InvoiceId
            External Chargebee invoice id (not a prefixed UUID).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerInvoicePdfResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.get_invoice_pdf(
                employer_id="empr_abc123def456",
                invoice_id="INV-00042",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_invoice_pdf(employer_id, invoice_id, request_options=request_options)
        return _response.data

    async def get_payroll_access_setup(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PayrollAccessSetupStatusResponse:
        """
        Return whether the employer has submitted payroll access setup.

        Parameters
        ----------
        employer_id : EmployerId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayrollAccessSetupStatusResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.get_payroll_access_setup(
                employer_id="empr_abc123def456",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_payroll_access_setup(employer_id, request_options=request_options)
        return _response.data

    async def submit_payroll_access_setup(
        self,
        employer_id: EmployerId,
        *,
        employees_in_payroll_acknowledged: bool,
        payroll_data_impacts_eligibility_acknowledged: bool,
        classifications_accurate: bool,
        all_benefit_eligible_employees_present: bool,
        is_controlled_group: bool,
        access_method: AccessMethod,
        has_additional_payroll_system: bool,
        classification_correction_source: typing.Optional[ClassificationCorrectionSource] = OMIT,
        misclassified_employee_names: typing.Optional[typing.Sequence[str]] = OMIT,
        remaining_employee_action: typing.Optional[RemainingEmployeeAction] = OMIT,
        missing_employee_resolution: typing.Optional[MissingEmployeeResolution] = OMIT,
        same_payroll_covers_other_eins: typing.Optional[bool] = OMIT,
        login_url: typing.Optional[str] = OMIT,
        username: typing.Optional[str] = OMIT,
        phone: typing.Optional[str] = OMIT,
        password: typing.Optional[str] = OMIT,
        integration_confirmed: typing.Optional[bool] = OMIT,
        additional_access_method: typing.Optional[AdditionalAccessMethod] = OMIT,
        additional_login_url: typing.Optional[str] = OMIT,
        additional_username: typing.Optional[str] = OMIT,
        additional_phone: typing.Optional[str] = OMIT,
        additional_password: typing.Optional[str] = OMIT,
        additional_integration_confirmed: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PayrollAccessSetupStatusResponse:
        """
        Submit the employer's payroll access setup answers.

        Parameters
        ----------
        employer_id : EmployerId

        employees_in_payroll_acknowledged : bool
            Attestation that all benefit-eligible employees appear in the payroll system.

        payroll_data_impacts_eligibility_acknowledged : bool
            Attestation that changes to payroll data affect benefit eligibility.

        classifications_accurate : bool
            Attestation that employee classifications in payroll are accurate. Set `false` to report corrections in the fields below.

        all_benefit_eligible_employees_present : bool
            Attestation that every benefit-eligible employee is present in payroll.

        is_controlled_group : bool
            Whether this employer belongs to a controlled group of related entities.

        access_method : AccessMethod

        has_additional_payroll_system : bool
            Whether a second payroll system is in use. When `true`, supply the `additional_*` fields below.

        classification_correction_source : typing.Optional[ClassificationCorrectionSource]
            Where corrected classifications come from, when `classifications_accurate` is `false`.

        misclassified_employee_names : typing.Optional[typing.Sequence[str]]
            Names of employees whose payroll classification needs correcting.

        remaining_employee_action : typing.Optional[RemainingEmployeeAction]
            How to handle employees still missing from the payroll system.

        missing_employee_resolution : typing.Optional[MissingEmployeeResolution]
            How any missing employees will be added, when some are absent.

        same_payroll_covers_other_eins : typing.Optional[bool]
            Whether this payroll system also covers other EINs in the controlled group.

        login_url : typing.Optional[str]
            Sign-in URL for the payroll system.

        username : typing.Optional[str]
            Username Vitable should use to access the payroll system.

        phone : typing.Optional[str]
            Phone number used for payroll-system verification codes.

        password : typing.Optional[str]
            Password Vitable should use to access the payroll system.

        integration_confirmed : typing.Optional[bool]
            Whether the payroll integration has been confirmed as working.

        additional_access_method : typing.Optional[AdditionalAccessMethod]
            How Vitable will access the second payroll system.

        additional_login_url : typing.Optional[str]
            Sign-in URL for the second payroll system.

        additional_username : typing.Optional[str]
            Username Vitable should use for the second payroll system.

        additional_phone : typing.Optional[str]
            Phone number used for second payroll-system verification codes.

        additional_password : typing.Optional[str]
            Password Vitable should use for the second payroll system.

        additional_integration_confirmed : typing.Optional[bool]
            Whether the second payroll integration has been confirmed as working.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayrollAccessSetupStatusResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.submit_payroll_access_setup(
                employer_id="empr_abc123def456",
                employees_in_payroll_acknowledged=True,
                payroll_data_impacts_eligibility_acknowledged=True,
                classifications_accurate=True,
                all_benefit_eligible_employees_present=True,
                is_controlled_group=True,
                access_method="SELF_SETUP",
                has_additional_payroll_system=True,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.submit_payroll_access_setup(
            employer_id,
            employees_in_payroll_acknowledged=employees_in_payroll_acknowledged,
            payroll_data_impacts_eligibility_acknowledged=payroll_data_impacts_eligibility_acknowledged,
            classifications_accurate=classifications_accurate,
            all_benefit_eligible_employees_present=all_benefit_eligible_employees_present,
            is_controlled_group=is_controlled_group,
            access_method=access_method,
            has_additional_payroll_system=has_additional_payroll_system,
            classification_correction_source=classification_correction_source,
            misclassified_employee_names=misclassified_employee_names,
            remaining_employee_action=remaining_employee_action,
            missing_employee_resolution=missing_employee_resolution,
            same_payroll_covers_other_eins=same_payroll_covers_other_eins,
            login_url=login_url,
            username=username,
            phone=phone,
            password=password,
            integration_confirmed=integration_confirmed,
            additional_access_method=additional_access_method,
            additional_login_url=additional_login_url,
            additional_username=additional_username,
            additional_phone=additional_phone,
            additional_password=additional_password,
            additional_integration_confirmed=additional_integration_confirmed,
            request_options=request_options,
        )
        return _response.data

    async def list_payroll_deduction_statements(
        self,
        employer_id: EmployerId,
        *,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[PayrollDeductionStatement, EmployerPayrollDeductionStatementListResponse]:
        """
        Returns a paginated list of the employer's payroll-deduction statements, newest period first, each with its period, generation date, distinct employee count, total deduction, change-file link, and deduction frequency. Statements superseded by a later correction are excluded. The caller must be authorized for the employer; an unknown or unauthorized employer returns 404.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        limit : typing.Optional[Limit]
            Maximum number of statements per page

        page : typing.Optional[Page]
            Page number to retrieve (starts at 1)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[PayrollDeductionStatement, EmployerPayrollDeductionStatementListResponse]


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.employers.list_payroll_deduction_statements(
                employer_id="empr_abc123def456",
                limit=20,
                page=1,
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list_payroll_deduction_statements(
            employer_id, limit=limit, page=page, request_options=request_options
        )

    async def ensure_payroll_integration_email(
        self, employer_id: EmployerId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PayrollIntegrationEmailResponse:
        """
        Provision and return the employer's payroll integration email.

        Parameters
        ----------
        employer_id : EmployerId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PayrollIntegrationEmailResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.ensure_payroll_integration_email(
                employer_id="empr_abc123def456",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.ensure_payroll_integration_email(
            employer_id, request_options=request_options
        )
        return _response.data

    async def update_settings(
        self,
        employer_id: EmployerId,
        *,
        pay_frequency: DeductionFrequency,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> EmployerSettingsResponse:
        """
        Updates configuration settings for a specific employer. The employer must belong to the authenticated organization.

        Parameters
        ----------
        employer_id : EmployerId
            Unique employer identifier (empr_*)

        pay_frequency : DeductionFrequency

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        EmployerSettingsResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.update_settings(
                employer_id="empr_abc123def456",
                pay_frequency="bi_weekly",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_settings(
            employer_id, pay_frequency=pay_frequency, request_options=request_options
        )
        return _response.data

    async def list_hris_providers(
        self, *, request_options: typing.Optional[RequestOptions] = None
    ) -> OrganizationHrisProvidersResponse:
        """
        Returns the distinct HRIS/payroll providers across the same book `GET /v1/employers` returns, sorted for display. Use these as the values for the employers list's `hris_provider` filter — filter on `provider`, show `provider_label`. The stored providers are free text, so they cannot be enumerated in advance.

        Parameters
        ----------
        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        OrganizationHrisProvidersResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.employers.list_hris_providers()


        asyncio.run(main())
        """
        _response = await self._raw_client.list_hris_providers(request_options=request_options)
        return _response.data

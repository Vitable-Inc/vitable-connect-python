# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel


class HrisConnection(UniversalBaseModel):
    provider: str = pydantic.Field()
    """
    Id of the HRIS/payroll provider the employer is connected to (e.g. `paylocity`).
    """

    provider_label: str = pydantic.Field()
    """
    Display name of that provider (e.g. `Paylocity`).
    """

    status: str = pydantic.Field()
    """
    Connection status reported by the integration.
    """

    last_sync_on: typing.Optional[dt.datetime] = pydantic.Field(default=None)
    """
    When the last sync completed, or null when none has.
    """

    synced_row_count: typing.Optional[int] = pydantic.Field(default=None)
    """
    Rows in the latest completed sync, or null when none has.
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow

# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

import pydantic
from ..core.pydantic_utilities import IS_PYDANTIC_V2, UniversalBaseModel
from .deduction_frequency import DeductionFrequency


class PayrollDeductionStatement(UniversalBaseModel):
    """
    One payroll-deduction statement row.

    Reads a :class:`PayrollDeductionStatementDTO` by attribute: the ``statement_id`` character field renders the
    prefixed id via ``str()``, and the date/datetime fields emit ISO-8601 strings.
    """

    statement_id: str = pydantic.Field()
    """
    Prefixed payroll-deduction-statement identifier (`pstmt_<base64-encoded-uuid>`).
    """

    period_start: dt.date = pydantic.Field()
    """
    Deduction period start date.
    """

    period_end: dt.date = pydantic.Field()
    """
    Deduction period end date.
    """

    run_date: dt.datetime = pydantic.Field()
    """
    When the statement was generated.
    """

    employee_count: int = pydantic.Field()
    """
    Distinct employees covered by the statement's entries.
    """

    total_deduction_cents: int = pydantic.Field()
    """
    Total payroll deduction for the period, in cents.
    """

    csv_file_url: typing.Optional[str] = pydantic.Field(default=None)
    """
    Download link for the change CSV, or null.
    """

    deduction_frequency: DeductionFrequency = pydantic.Field()
    """
    Deduction frequency code (e.g. `monthly`).
    
    * `weekly` - Weekly
    * `bi_weekly` - Bi Weekly
    * `semi_monthly` - Semi Monthly
    * `monthly` - Monthly
    """

    deduction_frequency_label: str = pydantic.Field()
    """
    Human-readable deduction frequency (e.g. `Monthly`).
    """

    if IS_PYDANTIC_V2:
        model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)  # type: ignore # Pydantic v2
    else:

        class Config:
            frozen = True
            smart_union = True
            extra = pydantic.Extra.allow

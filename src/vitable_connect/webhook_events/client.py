# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.pagination import AsyncPager, SyncPager
from ..core.request_options import RequestOptions
from ..types.created_after import CreatedAfter
from ..types.created_before import CreatedBefore
from ..types.event_id import EventId
from ..types.event_name import EventName
from ..types.limit import Limit
from ..types.list_webhook_event_deliveries_response import ListWebhookEventDeliveriesResponse
from ..types.page import Page
from ..types.resource_id import ResourceId
from ..types.resource_type import ResourceType
from ..types.webhook_event import WebhookEvent
from ..types.webhook_event_list_response import WebhookEventListResponse
from ..types.webhook_event_response import WebhookEventResponse
from .raw_client import AsyncRawWebhookEventsClient, RawWebhookEventsClient


class WebhookEventsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawWebhookEventsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawWebhookEventsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawWebhookEventsClient
        """
        return self._raw_client

    def list(
        self,
        *,
        created_after: typing.Optional[CreatedAfter] = None,
        created_before: typing.Optional[CreatedBefore] = None,
        event_name: typing.Optional[EventName] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        resource_id: typing.Optional[ResourceId] = None,
        resource_type: typing.Optional[ResourceType] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SyncPager[WebhookEvent, WebhookEventListResponse]:
        """
        Retrieves a paginated list of webhook events for the authenticated organization. Supports filtering by event name, resource type, resource ID, and date range.

        Parameters
        ----------
        created_after : typing.Optional[CreatedAfter]

        created_before : typing.Optional[CreatedBefore]

        event_name : typing.Optional[EventName]
            * `enrollment.accepted` - Enrollment Accepted
            * `enrollment.terminated` - Enrollment Terminated
            * `enrollment.termination_rescheduled` - Enrollment Termination Rescheduled
            * `enrollment.elected` - Enrollment Elected
            * `enrollment.granted` - Enrollment Granted
            * `enrollment.waived` - Enrollment Waived
            * `enrollment.started` - Enrollment Started
            * `employee.eligibility_granted` - Employee Eligibility Granted
            * `employee.eligibility_terminated` - Employee Eligibility Terminated
            * `employee.deactivated` - Employee Deactivated
            * `employee.deduction_created` - Employee Deduction Created

        limit : typing.Optional[Limit]

        page : typing.Optional[Page]

        resource_id : typing.Optional[ResourceId]

        resource_type : typing.Optional[ResourceType]
            * `enrollment` - Enrollment
            * `employee` - Employee
            * `employer` - Employer
            * `dependent` - Dependent
            * `plan_year` - Plan Year
            * `payroll_deduction` - Payroll Deduction

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SyncPager[WebhookEvent, WebhookEventListResponse]


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        response = client.webhook_events.list(
            limit=20,
            page=1,
        )
        for item in response:
            yield item
        # alternatively, you can paginate page-by-page
        for page in response.iter_pages():
            yield page
        """
        return self._raw_client.list(
            created_after=created_after,
            created_before=created_before,
            event_name=event_name,
            limit=limit,
            page=page,
            resource_id=resource_id,
            resource_type=resource_type,
            request_options=request_options,
        )

    def get(
        self, event_id: EventId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> WebhookEventResponse:
        """
        Retrieves a single webhook event by its prefixed ID. Returns 404 if the event does not exist or belongs to a different organization.

        Parameters
        ----------
        event_id : EventId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookEventResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.webhook_events.get(
            event_id="event_id",
        )
        """
        _response = self._raw_client.get(event_id, request_options=request_options)
        return _response.data

    def list_deliveries(
        self, event_id: EventId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ListWebhookEventDeliveriesResponse:
        """
        Retrieves all delivery attempts for a webhook event. Returns up to 100 deliveries. Each delivery includes a computed status field (Pending, In Progress, Delivered, or Failed).

        Parameters
        ----------
        event_id : EventId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListWebhookEventDeliveriesResponse


        Examples
        --------
        from vitable_connect import VitableConnect

        client = VitableConnect(
            api_key="YOUR_API_KEY",
        )
        client.webhook_events.list_deliveries(
            event_id="event_id",
        )
        """
        _response = self._raw_client.list_deliveries(event_id, request_options=request_options)
        return _response.data


class AsyncWebhookEventsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawWebhookEventsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawWebhookEventsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawWebhookEventsClient
        """
        return self._raw_client

    async def list(
        self,
        *,
        created_after: typing.Optional[CreatedAfter] = None,
        created_before: typing.Optional[CreatedBefore] = None,
        event_name: typing.Optional[EventName] = None,
        limit: typing.Optional[Limit] = None,
        page: typing.Optional[Page] = None,
        resource_id: typing.Optional[ResourceId] = None,
        resource_type: typing.Optional[ResourceType] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AsyncPager[WebhookEvent, WebhookEventListResponse]:
        """
        Retrieves a paginated list of webhook events for the authenticated organization. Supports filtering by event name, resource type, resource ID, and date range.

        Parameters
        ----------
        created_after : typing.Optional[CreatedAfter]

        created_before : typing.Optional[CreatedBefore]

        event_name : typing.Optional[EventName]
            * `enrollment.accepted` - Enrollment Accepted
            * `enrollment.terminated` - Enrollment Terminated
            * `enrollment.termination_rescheduled` - Enrollment Termination Rescheduled
            * `enrollment.elected` - Enrollment Elected
            * `enrollment.granted` - Enrollment Granted
            * `enrollment.waived` - Enrollment Waived
            * `enrollment.started` - Enrollment Started
            * `employee.eligibility_granted` - Employee Eligibility Granted
            * `employee.eligibility_terminated` - Employee Eligibility Terminated
            * `employee.deactivated` - Employee Deactivated
            * `employee.deduction_created` - Employee Deduction Created

        limit : typing.Optional[Limit]

        page : typing.Optional[Page]

        resource_id : typing.Optional[ResourceId]

        resource_type : typing.Optional[ResourceType]
            * `enrollment` - Enrollment
            * `employee` - Employee
            * `employer` - Employer
            * `dependent` - Dependent
            * `plan_year` - Plan Year
            * `payroll_deduction` - Payroll Deduction

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AsyncPager[WebhookEvent, WebhookEventListResponse]


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            response = await client.webhook_events.list(
                limit=20,
                page=1,
            )
            async for item in response:
                yield item

            # alternatively, you can paginate page-by-page
            async for page in response.iter_pages():
                yield page


        asyncio.run(main())
        """
        return await self._raw_client.list(
            created_after=created_after,
            created_before=created_before,
            event_name=event_name,
            limit=limit,
            page=page,
            resource_id=resource_id,
            resource_type=resource_type,
            request_options=request_options,
        )

    async def get(
        self, event_id: EventId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> WebhookEventResponse:
        """
        Retrieves a single webhook event by its prefixed ID. Returns 404 if the event does not exist or belongs to a different organization.

        Parameters
        ----------
        event_id : EventId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        WebhookEventResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.webhook_events.get(
                event_id="event_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(event_id, request_options=request_options)
        return _response.data

    async def list_deliveries(
        self, event_id: EventId, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ListWebhookEventDeliveriesResponse:
        """
        Retrieves all delivery attempts for a webhook event. Returns up to 100 deliveries. Each delivery includes a computed status field (Pending, In Progress, Delivered, or Failed).

        Parameters
        ----------
        event_id : EventId

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListWebhookEventDeliveriesResponse


        Examples
        --------
        import asyncio

        from vitable_connect import AsyncVitableConnect

        client = AsyncVitableConnect(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.webhook_events.list_deliveries(
                event_id="event_id",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_deliveries(event_id, request_options=request_options)
        return _response.data
